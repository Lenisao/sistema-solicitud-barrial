spring.application.name=ApiPrototipo

# URL de conexi�n (ajusta seg�n tu entorno)
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=Prototipo;encrypt=false

# Credenciales
spring.datasource.username=sa
spring.datasource.password=Enero241975

# Driver de SQL Server
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# Habilitar JdbcTemplate
spring.datasource.initialization-mode=always

# Opcional: mostrar SQL en consola
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Opcional: si no usas JPA, puedes ignorar esto
#spring.jpa.hibernate.ddl-auto=none

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect